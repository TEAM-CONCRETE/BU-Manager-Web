name: Create Issue from Jira
on:
  repository_dispatch:
    types: [jira-issue]

permissions:
  issues: write
  contents: read

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - name: Generate issue body (safe bash version)
        run: |
          # Jira에서 넘어온 payload 받기
          DESC="${{ github.event.client_payload.description }}"
          TODO="${{ github.event.client_payload.todo }}"
          REPORTER="${{ github.event.client_payload.reporter }}"
          KEY="${{ github.event.client_payload.key }}"
          URL="${{ github.event.client_payload.url }}"

          # \n, \\n → 실제 줄바꿈으로 치환
          DESC=$(printf "%b" "$DESC")
          TODO=$(printf "%b" "$TODO")

          # Markdown 파일 생성
          {
            echo "## 🚀 Description"
            echo "> ${DESC}"
            echo ""
            echo "---"
            echo "### ✅ To-Do List"
            echo "${TODO}" | while IFS= read -r line; do
              [ -n "$line" ] && echo "- [ ] $line"
            done
            echo ""
            echo "---"
            echo "### 👤 담당자"
            echo "$REPORTER"
            echo ""
            echo "### 🔗 Jira Link"
            echo "[**$KEY**]($URL)"
          } > issue-body.md

      - name: Create issue from generated file
        uses: peter-evans/create-issue-from-file@v4
        with:
          title: "[${{ github.event.client_payload.key }}] ${{ github.event.client_payload.summary }}"
          content-filepath: ./issue-body.md
          labels: |
            jira
            ${{ github.event.client_payload.type }}
